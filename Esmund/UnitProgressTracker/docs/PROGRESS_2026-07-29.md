# Unit Surface Viewer — progress (2026-07-29)

**Stable base:** v1.0.23 · marked end of session  
**Archive:** `releases/UnitSurfaceViewer_Source_v1.0.23_2026-07-29.zip`  
**SHA256:** `0B13E4267B2BB8443279F5C619A1CE0DA45484A1320B8DF5B7EAFE14B6F4C0E3`  
**OneDrive copy:** `C:\Users\esmun\OneDrive\Ce3\7-29\UnitSurfaceViewer_Source_v1.0.23_2026-07-29.zip`

## Tonight’s work

### BOM page + shell folder maker (v1.0.22)
- **BOM tab** in left panel (Surfaces | BOM)
- **Import BOM…** — reads `BOM_FLAT` `.xlsx` (Sheet1, 7 kept columns)
- **391- shell folder plan** under `Shell/Skid NN/NN XX/<assembly name>/`
  - Segment order from **reversed** skid bracket tokens (`[FR-MB]` → `01 MB`, `02 FR`)
  - Folder name = Description + Ext. Description (Windows-safe, max 120 chars)
  - Same part on same segment → one folder; different parts → separate folders
- **Exclude by description** (not part-number prefix): DOOR, SUMP DRAIN, FLOOR DRAIN, latches, ISO PLT, TEST COVER, DRAIN PAN NIPPLE KIT, ASY F GA-SPC, etc.
- **Coil panels** with `Segment = <--` — no folder; red alert lists misplaced lines
- **SQ custom** assemblies — amber row highlight + `SQ custom` tag
- **Set / relocate shell root** — user picks export root; persists in project JSON
- **Create Shell folders…** — empty nested folders for export targets
- **Open folder** per row — File Explorer shortcut
- BOM data saved in `.unit-surface-viewer/surface-data.json` with surface tracking

### BOM list sort & filter (v1.0.23)
- **Search** — part, description, segment, skid, folder path
- **Sort** — Shell nest, Part number, Description, Skid, Segment, Folder path
- **Skid / Segment** dropdown filters
- **SQ custom only** checkbox
- “Showing X of Y assemblies” when filtered
- Preferences saved in user `options.json` (`bomListDisplay`)

## Test BOM reference

File: `C:\Users\esmun\Desktop\test\BOM_FLAT_5E-030269-05_20260319_1057.xlsx`

| Metric | Count |
| --- | --- |
| 391- BOM rows | 112 |
| Planned export folders | 28 |
| Excluded (description rules) | 68 |
| Misplaced coil panels (`<--`) | 6 |
| Skipped (e.g. vestibule, no bracket match) | 8 |

Example path: `Shell/Skid 01/02 FR/DDFAN SKID 222EPFN CL3 118X160 2X2`

## Docs in repo

| File | Purpose |
| --- | --- |
| `docs/BOM_XLSX_IMPORT_MOCKUP.md` | Column map + tier filter proposal |
| `docs/BOM_391_PARTS_REVIEW.md` | All 391- parts with kept fields |
| `scripts/generate-bom-mockup.js` | Regenerate mockup doc from xlsx |
| `scripts/generate-391-review.js` | Regenerate 391 review doc |

## Key source files (new/changed)

| File | Role |
| --- | --- |
| `src/bom-folder-maker.js` | Filter, segment/skid parse, folder plan |
| `src/bom-list-display.js` | Sort/filter for BOM list |
| `src/bom-page.js` | BOM tab UI |
| `src/app.js` | View toggle, BOM handlers |
| `main.js` / `preload.js` | xlsx import, mkdir, open explorer IPC |

## Deferred (morning / later)

- Link/copy `.iam` assemblies into shell folders
- Full BOM page tiers (291-, 386-, hidden prefixes)
- BOM ↔ surface linking in project JSON
- Verify DOOR / exclusion list on real units
- Vestibule segment rule (currently skipped when not in skid bracket)
- More exclusion patterns as review continues

## Restore tomorrow

```powershell
cd UnitSurfaceViewer
npm install
pip install pywin32
npm start
```

If zip came from OneDrive/USB, unblock the folder before running (Properties → Unblock), or run from the Cursor tree directly.

Do **not** open `index.html` in a browser — use `npm start` (Electron).

## Prior stable base

v1.0.21 (2026-07-29) — startup scan fix, preload bridge, options/theme/viewer polish. Superseded by v1.0.23.
