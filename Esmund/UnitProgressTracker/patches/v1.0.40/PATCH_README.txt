Patch v1.0.40 — current build (keeper)
=======================================

Includes all v1.0.34–v1.0.39 fixes (BOM Config, replace scan, py -3, Inventor path matching).

New in v1.0.40:
  Replace surface confirm: OK = pick .iam file, Cancel = cancel (no folder option)

Apply: run Apply-Patch.bat, then npm start. Header shows v1.0.40.
